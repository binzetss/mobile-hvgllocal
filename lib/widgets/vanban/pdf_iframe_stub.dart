import 'package:flutter/material.dart';

class PdfIframeWidget extends StatelessWidget {
  final String url;
  const PdfIframeWidget({super.key, required this.url});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
